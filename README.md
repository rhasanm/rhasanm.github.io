# Getting Started

```bash
git clone git@github.com:Sohando/sohando.github.io.git
cd sohando.github.io
git submodule init
git submodule sync
# git submodule update
hugo server # (Ah! I'm using HUGO static site generators, never mentioned. Please install this first if you already haven't!!)
```
